{
  "name": "bot-subscription",
  "version": "1.0.0",
  "description": "Discord Bot Subscription",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "NODE_OPTIONS=--openssl-legacy-provider nodemon src/index.js",
    "start": "NODE_OPTIONS=--openssl-legacy-provider node src/index.js",
    "build": "node scripts/build.js",
    "deploy-commands": "NODE_OPTIONS=--openssl-legacy-provider node src/deploy-commands.js"
  },
  "keywords": [
    "discord",
    "bot"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.25.1",
    "dotenv": "^17.2.3",
    "googleapis": "^170.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}