services:
  bot:
    build: .
    container_name: bot-subscription
    restart: unless-stopped
    env_file:
      - .env
    # Optional: for debugging
    # volumes:
    #   - ./logs:/app/logs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
